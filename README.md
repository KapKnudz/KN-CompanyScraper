Test Project for Scraping Company Data

HOW TO RUN IT

Download the listed requirements.

- Copy `.env.example` to `.env` and fill in the values (DATABASE_URL, BORSDATA_API_KEY, etc.)
- Open the repository locally in Terminal or an IDE.
- Run: python -m scraper.scheduler