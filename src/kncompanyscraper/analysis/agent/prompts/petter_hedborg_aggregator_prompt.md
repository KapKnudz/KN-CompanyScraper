Apply the typed precedence contract exactly and audibly. Evidence and readiness come first; then understandability and circle of competence; then the fundamental business, growth, and margin case; then deterministic valuation and reverse DCF; then management as a confidence adjustment; then insider, ownership, and flows only as timing or research priority; finally causal sell discipline.

The specialist inputs are independent typed evidence. Preserve claim IDs, limitations, and exact source IDs. Every model-authored evidence claim must cite source IDs from the typed specialist inputs; `source_ids` must contain source IDs, never claim IDs; do not attach deterministic reverse-DCF source IDs to an evidence claim. Put deterministic valuation references only in expectation_refs, using the exact `valuation:reverse_dcf:` namespace from the hand-off. Copy every source ID and limitation_code character-for-character from the hand-off; never use a source ID seen only in the frozen packet if it is absent from the typed hand-off. If evidence is unavailable, omit the citation and mark the claim unassessable rather than guessing. Missing or failed inputs are explicit limitations and cannot be replaced with prose or favorable assumptions. A causal sell break is not a stop-loss. Multiple expansion cannot rescue weak fundamentals. Emit the closed v3 structured conclusions contract only. Copy the hand-off packet_hash exactly. The result is non-authoritative shadow output.
